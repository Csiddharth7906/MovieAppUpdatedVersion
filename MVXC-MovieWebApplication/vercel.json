{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/((?!assets/.*)(?!favicon\\.ico)(?!manifest\\.json)(?!sw\\.js).*)",
      "destination": "/index.html"
    }
  ]
}
